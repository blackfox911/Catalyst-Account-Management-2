## CATALYST ™ Accounts (Streamlit)

### Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

### Deploy on Streamlit Cloud

- Ensure `requirements.txt` is in the repo root so Streamlit Cloud installs dependencies (including `plotly`).
